if ENABLE_SHARED
noinst_LTLIBRARIES += _inheritance.la
_inheritance_la_SOURCES = boost.python/inheritance/Inheritance.cpp
# use -rpath to make sure that a shared lib is built
_inheritance_la_LDFLAGS = -shared -module -avoid-version -no-undefined -rpath /nowhere
_inheritance_la_LIBADD = $(ESPRESSO_BOOST_LIBS) $(PYTHON_LIBS)
endif
