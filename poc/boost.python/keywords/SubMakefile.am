if ENABLE_SHARED
noinst_LTLIBRARIES += _keywords.la
_keywords_la_SOURCES = \
	boost.python/keywords/_keywords.cpp
# use -rpath to make sure that a shared lib is built
_keywords_la_LDFLAGS = -shared -module -avoid-version -no-undefined -rpath /nowhere
_keywords_la_LIBADD = $(ESPRESSO_BOOST_LIBS) $(PYTHON_LIBS)
endif

