if ENABLE_SHARED
noinst_LTLIBRARIES += _real3D.la
_real3D_la_SOURCES = \
	boost.python/real3D/Real3D.cpp
# use -rpath to make sure that a shared lib is built
_real3D_la_LDFLAGS = -shared -module -avoid-version -no-undefined -rpath /nowhere
_real3D_la_LIBADD = $(ESPRESSO_BOOST_LIBS) $(PYTHON_LIBS)
endif
