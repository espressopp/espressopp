# these get generated by the ut script
tests_objects = $(tests:%.cpp=%.lo)
tests_executables = $(tests:%.cpp=%$(EXEEXT))

# get them distributed
EXTRA_DIST=$(tests)

# get them cleaned
MOSTLYCLEANFILES=$(tests_objects) $(tests_executables)

# define the tests
tests = TestTupleVector.cpp TestTimer.cpp

.PHONE: ut
ut: $(top_builddir)/src/libespresso_common.la
	$(top_builddir)/bin/utexec.sh --srcdir $(srcdir) $(tests)
