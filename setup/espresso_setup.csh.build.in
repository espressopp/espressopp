# Shell-script that sets the required environment variables to run 
# ESPResSo++ from the build path.
# @configure_input@
#
# Please source this script in the shell that you use to execute
# espresso, by using
#
# source @abs_top_builddir@/espresso_setup.csh
#
set abs_top_builddir="@abs_top_builddir@"

if ($?PYTHONPATH) then
    setenv PYTHONPATH ${PYTHONPATH}:${abs_top_builddir}
else
    setenv PYTHONPATH ${abs_top_builddir}
endif
echo "PYTHONPATH=${PYTHONPATH}"

set espp_path=${abs_top_builddir}/bin
if ($?PATH) then
   setenv PATH ${PATH}:${espp_path}
else
   setenv PATH ${espp_path}
endif
echo "PATH=${PATH}"
